## 1. 什么是位置编码？

简单来说，**位置编码（Positional Encoding）就是给输入的词贴上一张“序号标签”**。

在自然语言中，词语的顺序对句意至关 direction 关重要（比如“张三打李四”和“李四打张三”，用词完全一样，但含义截然相反）。由于 Transformer 架构的核心模块——自注意力机制（Self-Attention）是并行计算所有词的，它在处理文本时就像把一句话里的词全部扔进一个“词袋”里，只计算词与词之间的相关性，**默认完全没有方向和先后顺序概念**。

位置编码的作用，就是将词的“位置/顺序信息”转化为向量，与词本身的语义向量融合在一起，让模型在计算语义的同时也能识别出每个词在句子中的物理位置。

---

## 2. 为什么在 Transformer 中它是必需的？

1. **自注意力机制是“排列不变”的（Permutation Invariant）：**
在没有位置编码的情况下，无论打乱输入序列中词的顺序，Self-Attention 算出来的上下文语义表达都是完全一致的。没有位置编码，Transformer 就无法理解语法结构和语序逻辑。
2. **替换传统循环结构（RNN）的必然要求：**
传统的 RNN/LSTM 是按时间步“逐字读取”文本的，天然带有时间先后顺序；而 Transformer 为了实现大规模并行训练抛弃了循环结构，因此必须通过显式注入位置编码来补充丢失的位置信息。

---

## 3. 位置编码的常见实现方式

#### 1. 绝对位置编码（Absolute Positional Encoding）

直接为序列中的每个绝对位置（第 1 个词、第 2 个词……）分配唯一的 位置向量。

* **正弦/余弦周期函数编码（Sinusoidal Encoding，原论文 Transformer 使用）：**
* **原理**：利用不同频率的 `sin` 和 `cos` 函数公式直接计算出每个位置的固定向量值。
* **优点**：无需参数训练，计算快；且通过三角函数的和差公式，模型具有推导出相对位置关系的数学潜能。


* **可学习的位置编码（Learned Absolute Position Embedding，如 BERT、GPT-2）：**
* **原理**：将位置向量作为模型的可训练参数（类似于词表 Embeddings），在模型训练过程中自动学习每个位置的最佳表示。
* **缺点**：泛化能力受限，无法很好地处理超出训练时预设最大长度的序列。



#### 2. 相对位置编码（Relative Positional Encoding）

不再关注某个词的绝对序号，而是**关注词与词之间的距离（相对偏移量 $i - j$）**。

# 二、在长文本大模型中，为什么 RoPE 和 ALiBi 比传统的绝对位置编码具有更好的外推能力（Length Extrapolation）？

在长文本大模型（LLM）中，**外推能力（Length Extrapolation）** 指的是：模型在较短的文本长度（如 4k）上训练后，能否在不重新训练的前提下，直接推断和处理更长的文本（如 8k、32k 甚至更长），且注意力分布不崩溃、困惑度（PPL）不暴涨。

传统的绝对位置编码在面对超出训练长度的输入时表现较差，而 **RoPE** 和 **ALiBi** 凭借其独特的数学机制实现了更优越的外推性。

---

## 4. 传统绝对位置编码为什么外推性差？

绝对位置编码（无论是 BERT/GPT-2 的可学习 Embedding，还是 Transformer 原论文的 Sinusoidal 编码）的主要缺陷在于：**它将位置绑定在固定的绝对序号（如 $pos = 1, 2, \dots, 4096$）上。**

* **可学习编码（Learned Position Embedding）：**
训练时最大长度设为 $N$（如 4096），超过 $N$ 的位置（如 $pos = 4097$）在训练集中**从未出现过**，对应的位置向量未被更新，推理时模型无法识别这些未知的位置向量。
* **正弦/余弦编码（Sinusoidal Position Encoding）：**
虽然理论上函数可以无界外推到任意大 $pos$，但由于注意力计算时取的是绝对位置向量的内积，超过训练长度的绝对位置组合（如 $pos_i=5000, pos_j=5010$）会产生未曾学习到的高频震荡或未见过的点积分布，导致 Query 和 Key 的内积数值异常（如过大），激活函数的注意力权重趋于极值，使得注意力机制失效。

---

## 5. RoPE（旋转位置编码）的外推优势

RoPE（Rotary Position Embedding）通过旋转矩阵将位置信息融入 Query ($Q$) 和 Key ($K$) 的复数空间旋转中，使得 $Q$ 和 $K$ 的内积**只依赖于两者的相对距离 $m - n$**，而非绝对位置。

* **复数旋转机制与高频衰减：**
RoPE 将向量按维度两两分组，不同维度乘以不同频率的旋转因子 $\theta_d$。其中高频通道（对应小 $\theta$）捕捉近距离上下文，低频通道（对应大 $\theta$）捕捉长距离上下文。
* **外推痛点与扩展性：**
原始 RoPE 在输入长度大幅超过训练长度时，高频通道的旋转相位会快速超出训练时的取值范围（超出一周或多次旋转），导致模型对相对距离敏感度失真。
* **为什么它外推性好？**
虽然原始 RoPE 也有外推上限，但因为其**严密的相对位置数学表达**，非常容易通过简单的频率插值进行外推：
1. **位置插值（Position Interpolation, PI）：** 将超出长度的位置等比例压缩回训练窗口内（如将 $0 \sim 8192$ 缩放到 $0 \sim 4096$），通过插值而非外推解决长文本问题。
2. **NTK-aware Scaled RoPE / YaRN：** 保持高频通道不变（保留高分辨率的近距离语义），仅对低频通道进行旋转基数外推，从而在不损失微观语义的前提下，无缝外推至数万甚至数十万 Token 的上下文。



---

## 6. ALiBi（带有线性偏置的注意力）的外推优势

ALiBi（Attention with Linear Biases）放弃了在向量（Embedding）层面注入位置信息的传统做法，直接在计算注意力得分（Attention Score）时施加一个**随相对距离线性递增的负偏置（惩罚项）**。

其计算公式为：


$$\text{Attention Score}_{i, j} = \frac{q_i k_j^\top}{\sqrt{d}} - m \cdot \vert{}i - j\vert{}$$

其中 $m$ 是为不同注意力头固定设置的几何级数衰减因子（比率常量），$\vert{}i - j\vert{}$ 为 Token 之间的相对距离。

ALiBi 具有极强外推能力的核心原因如下：

* **不依赖位置 Embedding：**
模型根本不学习或计算任何位置向量，因此不存在“未见过的位置编号”这一概念。
* **自然衰减（Decay）约束：**
距离越远的两个 Token，其偏置惩罚 $- m \cdot \vert{}i - j\vert{}$ 就越大，注意力权重会自动向 0 衰减。这种平滑的线性惩罚保证了即使遇到非常远的 Token（如 $pos = 10000$），Softmax 的输入数值也不会暴涨或产生剧烈波动。
* **零样本外推（Zero-shot Extrapolation）：**
在短文本（如 1024）上训练的 ALiBi 模型，可以直接在推理时处理更长文本（如 2048 或更长），注意力机制依然稳定，困惑度不会暴涨。

---

### 总结对比

| 位置编码类型 | 核心表达形式 | 超出训练长度时的表现 | 外推原理 / 解决方案 |
| --- | --- | --- | --- |
| **传统绝对位置编码** | 绝对位置序号 / 向量 | 极差，语义崩溃或无法运行 | 无法自然外推，超出长度后效果急剧下降 |
| **RoPE** | 向量旋转（复数空间相对位置） | 较好，配合插值算法可极佳 | 具备严谨的相对位置数学特性，可通过 **PI / NTK-aware / YaRN** 实现超长上下文扩展 |
| **ALiBi** | 注意力矩阵上的距离负偏置 | 极佳 | 摒弃位置向量，通过相对距离的线性惩罚天然约束注意力分布，支持零样本外推 |

## 7.ALiBi 外推能力强，但为什么当前的顶级开源大模型（如 LLaMA 3、Qwen 2.5）依然普遍选择 RoPE 而不是 ALiBi？
尽管 ALiBi 在外推能力和工程实现上非常优雅，但从 **LLaMA 2/3、Qwen 2/2.5、Mistral 到 DeepSeek** 等绝大多数主流顶级大模型，最终都统一转向了 **RoPE（或其衍生变体如 YaRN / SuPE）**。

这种选择主要源于以下几个核心维度的权衡：

---

### 1. 表达能力与注意力衰减假设（根本原因）

ALiBi 的核心假设是：**距离越远的 Token，相关性越低（Inductive Bias: Distance Penalty）**。

* **ALiBi 的局限：**
ALiBi 通过线性惩罚强制让远距离 Token 的 Attention Score 衰减。但在许多复杂任务中，**“距离远”不等于“不重要”**：
* **大海捞针（Needle in a Haystack）**：检索数万 Token 之前的关键指令或事实。
* **代码与长逻辑推理**：文件开头的函数定义、类声明或全局变量，需要在几十 KB 代码之后的末尾被频繁引用。
* **长文档跨段落对比**：开头的第一章和结尾的最后一章可能存在直接的因果或对比关系。


在这些场景下，ALiBi 的强行线性衰减会**无差别地压制远距离关键信息的注意力**，造成长距离信息获取能力下降。
* **RoPE 的优势：**
RoPE **只提供相对位置的旋转语义，不对远距离相关性做强行的强制衰减**。模型可以自由学习在远距离上依然保持高注意力（只要 Query 和 Key 的相位/方向一致）。这使得 RoPE 在处理复杂长文本、代码和深度推理时具有更高的上限。

---

### 2. RoPE 动态缩放技术的成熟（外推短板被补齐）

早期大家推崇 ALiBi，是因为原始 RoPE 在直接外推时表现较差。但随着大模型社区对 RoPE 几何性质的深度挖掘，一系列**无需重训或只需极少微调**的 RoPE 扩展方案迅速成熟：

* **位置插值（PI）、NTK-aware Scaled RoPE、YaRN、SuScaled RoPE 等技术**：
通过对不同频率通道（高频保留近距离高分辨率，低频平滑缩放适配长距离）进行非线性插值，RoPE 轻松实现了将上下文从 4k 扩展到 **128k 甚至 1M Token**（如 Qwen 2.5 具备原生 128k 上下文扩展能力）。
* **结果：**
ALiBi 最大的优势（“不需要缩放即可外推”）被 RoPE 社区通过算法补齐，而 RoPE 表达能力更强的优点则被保留了下来。

---

### 3. 多模态与二维/多维位置拓展性

随着大模型向多模态（Vision-Language / Audio / Video）演进，模型不仅要处理一维的文本序列，还要处理二维的图片像素块（Patches）和三维的视频帧。

* **ALiBi 的局限：**
ALiBi 的一维线性偏置偏向纯文本，很难自然且优雅地推广到二维（2D Image）或三维（3D Video）空间位置上。
* **RoPE 的优势：**
RoPE 可以非常自然地推广为 **2D-RoPE / 3D-RoPE**（例如在 Qwen2-VL 中使用），只需将向量维度切分，分别对图像的 X 轴、Y 轴或视频的 Time 轴应用旋转矩阵即可，完美适配统一多模态架构。

---

### 4. 硬件与算力优化的生态惯性（FlashAttention）

现代大模型训练高度依赖 GPU 算力优化库，尤其是 **FlashAttention**：

* 早期 FlashAttention 在处理带自定义 Bias 矩阵（如 ALiBi 各种头不同的 slope 惩罚）时，显存开销和 IO 优化难度高于标准的 Query/Key 旋转乘法。
* RoPE 的位置信息是**直接嵌入在 $Q$ 和 $K$ 向量本身内部的**（Multiply in-place），在进入 Self-Attention 矩阵计算前就已完成，因此能够无缝享受 FlashAttention 等底层算子带来的极致加速与显存节省。

---

### 总结

| 维度 | ALiBi | RoPE（及其变体如 YaRN） |
| --- | --- | --- |
| **先验假设** | 假设距离越远越不重要（强衰减） | 不做预设，由数据自由驱动 |
| **长距离检索/代码推理** | 容易误杀远距离关键信息 | 表现优异，上限更高 |
| **超长上下文扩展** | 零样本原生支持（但长距离注意力受限） | 配合 PI/NTK/YaRN 可平滑扩至 128k~1M |
| **多模态扩展 (2D/3D)** | 较难直接扩展 | 极易扩展（2D/3D-RoPE） |
| **生态与硬件适配** | 需要额外的 Attention Bias 算子 | 原生适配 FlashAttention |

简而言之：**ALiBi 用表达能力的上限换取了免微调的外推方便；而 RoPE 配合现代化插值技术（YaRN 等），既保留了极高的语义表达上限，又解决了长文本外推问题。** 这正是顶级开源大模型普遍拥抱 RoPE 的根本原因。


## 8.举例说明ROPE的原理

用一个最直观的“钟表指针”例子，就能彻底搞懂 RoPE（Rotary Position Embedding，旋转位置编码）的核心原理。

---

### 1. 核心直觉：把词向量当成“钟表指针”

传统的位置编码是直接在词向量上**加**一个位置数值（比如 $x + pos$）。

而 RoPE 的核心思想是：**不通过加法，而是通过“旋转”词向量的角度，把位置信息融进去。**

* **语义（含义）**：由指针的**长度和初始方向**决定。
* **位置（序号）**：由指针被**旋转的角度**决定。

假设每一个词经过特征提取后，是一个二维平面上的向量（即一根针）。
我们规定：**位置每增加 1，指针就顺时针旋转一个固定的角度 $\theta$（比如 30度）。**

---

### 2. 用具体例子看 RoPE 怎么运作

假设我们有一句话：**“苹果 好吃”**

#### 第一步：映射语义向量（不带位置）

模型先把这两个词映射成基本的 Query ($Q$) 和 Key ($K$) 向量（假设均为二维向量）：

* **“苹果”**（在第 0 个位置）：$Q_0 = [1, 0]$（指针指向 3 点钟方向，角度为 $0^\circ$）
* **“好吃”**（在第 1 个位置）：$K_1 = [0.8, 0.6]$（指针初始方向为 $36.8^\circ$）

#### 第二步：根据位置进行“旋转”（注入位置信息）

设定位置旋转单位步长 $\theta = 30^\circ$：

1. **“苹果”位于位置 $m = 0$**：
* 旋转角度 = $0 \times 30^\circ = 0^\circ$
* 旋转后的 $\tilde{Q}_0$ 保持不变：仍然指向 $0^\circ$。


2. **“好吃”位于位置 $n = 1$**：
* 旋转角度 = $1 \times 30^\circ = 30^\circ$
* 旋转后的 $\tilde{K}_1$：在原来的 $36.8^\circ$ 基础上顺时针旋转 $30^\circ$，变成 $66.8^\circ$。



---

### 3. 奇迹发生的时刻：计算注意力（点积）

Transformer 计算两个词相关性（Attention Score）的方式，是计算 $Q$ 和 $K$ 的**点积（Dot Product）**。

在几何上，两个向量的点积公式为：


$$\text{点积} = \vert{}\tilde{Q}\vert{} \times \vert{}\tilde{K}\vert{} \times \cos(\text{两向量夹角})$$

我们来看“苹果”（位置 $m$）和“好吃”（位置 $n$）的点积计算：

1. $\tilde{Q}_m$ 的最终角度为：$\text{初始角}_Q + m \cdot \theta$
2. $\tilde{K}_n$ 的最终角度为：$\text{初始角}_K + n \cdot \theta$
3. 它们之间的**夹角**为：

$$\text{夹角} = (\text{初始角}_Q + m \cdot \theta) - (\text{初始角}_K + n \cdot \theta) = (\text{初始角}_Q - \text{初始角}_K) + (m - n) \cdot \theta$$



注意到没有？在算夹角时，绝对位置 $m$ 和 $n$ 消掉了，**只剩下了相对距离 $(m - n)$！**

* **绝对位置**：被各自旋转的度数隐式融合进了向量中；
* **相对位置**：在两者求点积（计算注意力）的瞬间，自然浮现出来（只由夹角里的 $m - n$ 决定）。

---

### 4. 推广到真实的高维向量（比如 4096 维）

现实中的大模型向量不是二维的，而是 4096 维甚至更高。RoPE 是怎么处理的呢？

* **两两分组**：把 4096 维的向量拆分成 2048 个独立的“二维平面”（第 1 和第 2 维一组，第 3 和第 4 维一组……）。
* **不同平面不同转速**：
* **前面的维度**（高频通道）：$\theta$ 非常大，转得很快，用来捕捉**近距离**的词关系（比如相邻词的语法组合）。
* **后面的维度**（低频通道）：$\theta$ 非常小，转得很慢，用来捕捉**长距离**的上下文逻辑。



---

### 总结

RoPE 的本质就是：**用“复数/向量旋转”将绝对位置赋予向量，利用“旋转角度相减”的三角函数性质，在计算注意力点积时自然提取出两者的相对位置关系。**

这既不用像传统方法那样强行相加扰乱语义，又完美保留了长文外推的几何数学特性。

## 9.请提供 RoPE 的数学旋转矩阵公式，并用 PyTorch 给出具体的代码实现。
### RoPE 的数学表达与旋转矩阵

RoPE 的核心思想是利用**二维旋转矩阵**对向量的相邻维度对逐一实施旋转。

#### 1. 二维旋转矩阵

对于一个二维向量 $\boldsymbol{x} = [x_1, x_2]^\top$，若要在平面内将其逆时针旋转 $\theta$ 角度，对应的旋转矩阵 $\mathbf{R}_{\theta}$ 定义为：

$$\mathbf{R}_{\theta} = \begin{pmatrix} \cos\theta & -\sin\theta \\ \sin\theta & \cos\theta \end{pmatrix}$$

将位置 $m$ 的信息注入向量 $\boldsymbol{x}$，只需将旋转角度设为 $m\theta$：

$$\mathbf{R}_{\theta, m} \boldsymbol{x} = \begin{pmatrix} \cos(m\theta) & -\sin(m\theta) \\ \sin(m\theta) & \cos(m\theta) \end{pmatrix} \begin{pmatrix} x_1 \\ x_2 \end{pmatrix} = \begin{pmatrix} x_1 \cos(m\theta) - x_2 \sin(m\theta) \\ x_2 \cos(m\theta) + x_1 \sin(m\theta) \end{pmatrix}$$

#### 2. 高维向量扩展（$d$ 维）

对于 $d$ 维的词向量 $\boldsymbol{x} = [x_1, x_2, x_3, x_4, \dots, x_{d-1}, x_d]^\top$（其中 $d$ 为偶数），RoPE 将其切分为 $\frac{d}{2}$ 个二维子空间，每个子空间赋予不同的旋转频率 $\theta_i$：

$$\theta_i = 10000^{-2(i-1)/d}, \quad i \in \left\{1, 2, \dots, \frac{d}{2}\right\}$$

整体的分块对角旋转矩阵 $\mathbf{R}_{\Theta, m}^d$ 形式如下：

$$\mathbf{R}_{\Theta, m}^d = \begin{pmatrix} \cos(m\theta_1) & -\sin(m\theta_1) & 0 & 0 & \cdots & 0 & 0 \\ \sin(m\theta_1) & \cos(m\theta_1) & 0 & 0 & \cdots & 0 & 0 \\ 0 & 0 & \cos(m\theta_2) & -\sin(m\theta_2) & \cdots & 0 & 0 \\ 0 & 0 & \sin(m\theta_2) & \cos(m\theta_2) & \cdots & 0 & 0 \\ \vdots & \vdots & \vdots & \vdots & \ddots & \vdots & \vdots \\ 0 & 0 & 0 & 0 & \cdots & \cos(m\theta_{d/2}) & -\sin(m\theta_{d/2}) \\ 0 & 0 & 0 & 0 & \cdots & \sin(m\theta_{d/2}) & \cos(m\theta_{d/2}) \end{pmatrix}$$

#### 3. 计算优化（向量化形式）

在实践中，模型不会显式构建这个稀疏的 $d \times d$ 矩阵，而是利用复数乘法或如下的向量化方式计算：

$$\mathbf{R}_{\Theta, m}^d \boldsymbol{x} = \boldsymbol{x} \odot \cos(m\mathbf{\Theta}) + \tilde{\boldsymbol{x}} \odot \sin(m\mathbf{\Theta})$$

其中：

* $\mathbf{\Theta} = [\theta_1, \theta_1, \theta_2, \theta_2, \dots, \theta_{d/2}, \theta_{d/2}]^\top$
* $\tilde{\boldsymbol{x}} = [-x_2, x_1, -x_4, x_3, \dots, -x_d, x_{d-1}]^\top$（即对相邻维度取反并交替交换）
* $\odot$ 表示逐元素相乘（Hadamard product）。

---

### PyTorch 代码实现

以下是符合 LLaMA / Qwen 工业级实现风格的简洁 PyTorch 代码：

```python
import torch
import torch.nn as nn

def precompute_freqs_cis(dim: int, seq_len: int, theta: float = 10000.0):
    """
    预计算旋转角度的 cos 和 sin 频率矩阵 (复数/极坐标形式或实数拆分形式)
    
    参数:
        dim: head_dim (每个注意力头的维度，必须为偶数)
        seq_len: 支持的最大序列长度
        theta: 基频超参数 (如 10000.0)
    """
    # 1. 计算每个维度组的频率: theta_i = 10000^(-2*(i-1)/d)
    # 维度形状: [dim // 2]
    freqs = 1.0 / (theta ** (torch.arange(0, dim, 2)[: (dim // 2)].float() / dim))
    
    # 2. 生成位置索引 t = [0, 1, 2, ..., seq_len - 1]
    # 维度形状: [seq_len]
    t = torch.arange(seq_len, device=freqs.device, dtype=torch.float32)
    
    # 3. 外积得到所有位置在各个维度上的角度 m * theta_i
    # 维度形状: [seq_len, dim // 2]
    freqs = torch.outer(t, freqs)
    
    # 4. 广播扩充至完整的 head_dim 长度: [cos(m*theta_1), cos(m*theta_1), cos(m*theta_2), ...]
    # 维度形状: [seq_len, dim]
    freqs = torch.cat([freqs, freqs], dim=-1)
    
    cos = freqs.cos()
    sin = freqs.sin()
    return cos, sin


def rotate_half(x: torch.Tensor) -> torch.Tensor:
    """
    实现 \tilde{x} 操作: 将向量后半部分取负后与前半部分拼合
    即把 [x1, x2, x3, x4] 转变为 [-x3, -x4, x1, x2]
    """
    x1 = x[..., : x.shape[-1] // 2]
    x2 = x[..., x.shape[-1] // 2 :]
    return torch.cat((-x2, x1), dim=-1)


def apply_rotary_pos_emb(x: torch.Tensor, cos: torch.Tensor, sin: torch.Tensor) -> torch.Tensor:
    """
    将预计算的 cos 和 sin 应用于输入的 Query 或 Key 向量
    
    参数:
        x: [batch_size, num_heads, seq_len, head_dim]
        cos: [seq_len, head_dim] -> 需要广播匹配 x 的形状
        sin: [seq_len, head_dim] -> 需要广播匹配 x 的形状
    """
    # 调整 cos 和 sin 形状以匹配 [batch_size, num_heads, seq_len, head_dim]
    cos = cos.unsqueeze(0).unsqueeze(1) # [1, 1, seq_len, head_dim]
    sin = sin.unsqueeze(0).unsqueeze(1) # [1, 1, seq_len, head_dim]
    
    # 执行旋转: x * cos + rotate_half(x) * sin
    return (x * cos) + (rotate_half(x) * sin)


# ==================== 测试代码 ====================
if __name__ == "__main__":
    batch_size = 2
    num_heads = 8
    seq_len = 16
    head_dim = 64  # 必须为偶数
    
    # 1. 模拟 Attention 里的 Query 向量
    q = torch.randn(batch_size, num_heads, seq_len, head_dim)
    
    # 2. 预计算位置编码表
    cos, sin = precompute_freqs_cis(dim=head_dim, seq_len=seq_len)
    
    # 3. 注入位置编码
    q_embed = apply_rotary_pos_emb(q, cos, sin)
    
    print(f"输入 Query 形状: {q.shape}")
    print(f"注入 RoPE 后 Query 形状: {q_embed.shape}")

```

## 10.先不用写代码，用5个token的向量和位置，使用rope公式推导一遍
为了方便手工推导并清晰呈现数值变化的几何过程，我们设置一组简单且符合标准的参数：

* **向量维度（$d$）**：设 $d = 2$（单个二维平面，只有一个频率 $\theta$）。
* **基础频率（$\theta$）**：设 $\theta = \frac{\pi}{6}$（即步长为 $30^\circ$）。
* **输入 Token 序列**：$5$ 个 Token，位置索引 $m \in \{0, 1, 2, 3, 4\}$。
* **假设语义向量（Query/Key）**：为了方便计算，设 $5$ 个 Token 拥有完全相同的初始二维向量 $\boldsymbol{x} = [1, 0]^\top$（模长为 $1$，初始角度为 $0^\circ$）。

---

### 一、 核心推导公式

根据 RoPE 的二维旋转公式，位置 $m$ 处的向量 $\boldsymbol{x}_m = [x_1, x_2]^\top$ 经过旋转后得到 $\tilde{\boldsymbol{x}}_m$：

$$\tilde{\boldsymbol{x}}_m = \begin{pmatrix} \cos(m\theta) & -\sin(m\theta) \\ \sin(m\theta) & \cos(m\theta) \end{pmatrix} \begin{pmatrix} x_1 \\ x_2 \end{pmatrix} = \begin{pmatrix} x_1 \cos(m\theta) - x_2 \sin(m\theta) \\ x_2 \cos(m\theta) + x_1 \sin(m\theta) \end{pmatrix}$$

因为我们的初始向量为 $x_1 = 1, x_2 = 0$，公式直接简化为：

$$\tilde{\boldsymbol{x}}_m = \begin{pmatrix} \cos(m \cdot \frac{\pi}{6}) \\ \sin(m \cdot \frac{\pi}{6}) \end{pmatrix}$$

---

### 二、 逐位置（5 个 Token）推导结果

我们将 $m = 0, 1, 2, 3, 4$ 分别代入公式中：

#### 1. Token 0（位置 $m = 0$）

* **旋转角度**：$0 \times 30^\circ = 0^\circ$
* **向量计算**：

$$\tilde{\boldsymbol{x}}_0 = \begin{pmatrix} \cos(0^\circ) \\ \sin(0^\circ) \end{pmatrix} = \begin{pmatrix} 1 \\ 0 \end{pmatrix}$$



#### 2. Token 1（位置 $m = 1$）

* **旋转角度**：$1 \times 30^\circ = 30^\circ$（即 $\frac{\pi}{6}$）
* **向量计算**：

$$\tilde{\boldsymbol{x}}_1 = \begin{pmatrix} \cos(30^\circ) \\ \sin(30^\circ) \end{pmatrix} = \begin{pmatrix} \frac{\sqrt{3}}{2} \\ \frac{1}{2} \end{pmatrix} \approx \begin{pmatrix} 0.866 \\ 0.500 \end{pmatrix}$$



#### 3. Token 2（位置 $m = 2$）

* **旋转角度**：$2 \times 30^\circ = 60^\circ$（即 $\frac{\pi}{3}$）
* **向量计算**：

$$\tilde{\boldsymbol{x}}_2 = \begin{pmatrix} \cos(60^\circ) \\ \sin(60^\circ) \end{pmatrix} = \begin{pmatrix} \frac{1}{2} \\ \frac{\sqrt{3}}{2} \end{pmatrix} \approx \begin{pmatrix} 0.500 \\ 0.866 \end{pmatrix}$$



#### 4. Token 3（位置 $m = 3$）

* **旋转角度**：$3 \times 30^\circ = 90^\circ$（即 $\frac{\pi}{2}$）
* **向量计算**：

$$\tilde{\boldsymbol{x}}_3 = \begin{pmatrix} \cos(90^\circ) \\ \sin(90^\circ) \end{pmatrix} = \begin{pmatrix} 0 \\ 1 \end{pmatrix}$$



#### 5. Token 4（位置 $m = 4$）

* **旋转角度**：$4 \times 30^\circ = 120^\circ$（即 $\frac{2\pi}{3}$）
* **向量计算**：

$$\tilde{\boldsymbol{x}}_4 = \begin{pmatrix} \cos(120^\circ) \\ \sin(120^\circ) \end{pmatrix} = \begin{pmatrix} -\frac{1}{2} \\ \frac{\sqrt{3}}{2} \end{pmatrix} \approx \begin{pmatrix} -0.500 \\ 0.866 \end{pmatrix}$$



---

### 三、 验证相对位置属性（注意力点积计算）

RoPE 最精妙的地方在于，当两个 Token 计算点积（Attention Score）时，绝对位置会自然消去，只保留相对位置。

假设我们要计算 **Token 4（Query，位置 $m=4$）** 与 **Token 1（Key，位置 $n=1$）** 之间的点积：

1. **方法一：直接用导出的旋转向量做内积（坐标点积）**

$$\text{Score}_{4, 1} = \tilde{\boldsymbol{x}}_4 \cdot \tilde{\boldsymbol{x}}_1 = (-0.500 \times 0.866) + (0.866 \times 0.500) = -0.433 + 0.433 = 0$$


2. **方法二：用三角函数展开（角度差值）**
根据三角恒等变换 $\cos(A)\cos(B) + \sin(A)\sin(B) = \cos(A - B)$：

$$\text{Score}_{4, 1} = \cos(m\theta)\cos(n\theta) + \sin(m\theta)\sin(n\theta) = \cos((m - n)\theta)$$



代入相对距离 $m - n = 4 - 1 = 3$：

$$\text{Score}_{4, 1} = \cos(3 \times 30^\circ) = \cos(90^\circ) = 0$$



两种推导方式的结果**完全一致**。这证明了即使初始向量相同，模型依然可以通过相对夹角 $3 \times 30^\circ = 90^\circ$ 准确感知到它们之间相隔了 $3$ 个 Token。

## 11.$$\text{Score}_{4, 1} = \cos(m\theta)\cos(n\theta) + \sin(m\theta)\sin(n\theta) = \cos((m - n)\theta)$$ 推导公式
这个公式的推导非常直观，它本质上就是**高中数学里的“两角差的余弦公式”**与**向量点积的坐标表示**相结合的结果。

我们分步来看它是怎么一步步展开并合并的：

---

### 第一步：回顾两个向量的点积计算

根据之前推导的结果，位置 $m$ 处的向量 $\tilde{\boldsymbol{x}}_m$ 和位置 $n$ 处的向量 $\tilde{\boldsymbol{x}}_n$ 分别为：

$$\tilde{\boldsymbol{x}}_m = \begin{pmatrix} \cos(m\theta) \\ \sin(m\theta) \end{pmatrix}, \quad \tilde{\boldsymbol{x}}_n = \begin{pmatrix} \cos(n\theta) \\ \sin(n\theta) \end{pmatrix}$$

两个二维向量 $\boldsymbol{a} = [a_1, a_2]^\top$ 和 $\boldsymbol{b} = [b_1, b_2]^\top$ 的点积（Dot Product）定义为对应坐标相乘再相加：

$$\boldsymbol{a} \cdot \boldsymbol{b} = a_1 b_1 + a_2 b_2$$

我们将 $\tilde{\boldsymbol{x}}_m$（做 Query）和 $\tilde{\boldsymbol{x}}_n$（做 Key）的坐标直接代入点积公式中：

* $a_1 = \cos(m\theta)$， $a_2 = \sin(m\theta)$
* $b_1 = \cos(n\theta)$， $b_2 = \sin(n\theta)$

因此，它们的点积（注意力得分 $\text{Score}_{m, n}$）就是：

$$\text{Score}_{m, n} = \tilde{\boldsymbol{x}}_m \cdot \tilde{\boldsymbol{x}}_n = \underbrace{\cos(m\theta)}_{a_1} \cdot \underbrace{\cos(n\theta)}_{b_1} + \underbrace{\sin(m\theta)}_{a_2} \cdot \underbrace{\sin(n\theta)}_{b_2}$$

这正是等式左半部分的由来！

---

### 第二步：套用三角函数恒等式（两角差的余弦公式）

在三角函数中，有一个非常经典的恒等变换公式——**余弦差角公式**：

$$\cos(\alpha - \beta) = \cos\alpha \cos\beta + \sin\alpha \sin\beta$$

我们令：

* $\alpha = m\theta$
* $\beta = n\theta$

将它们代入上面的恒等式右边：

$$\cos(m\theta) \cos(n\theta) + \sin(m\theta) \sin(n\theta) = \cos(m\theta - n\theta)$$

提取公因式 $\theta$，即可直接化简为：

$$= \cos((m - n)\theta)$$

---

### 总结

整个推导过程逻辑非常清晰：

1. **向量点积的坐标展开：** $\tilde{\boldsymbol{x}}_m \cdot \tilde{\boldsymbol{x}}_n = \cos(m\theta)\cos(n\theta) + \sin(m\theta)\sin(n\theta)$
2. **三角恒等式合并：** 利用 $\cos\alpha\cos\beta + \sin\alpha\sin\beta = \cos(\alpha - \beta)$，将式子化简为 $\cos((m - n)\theta)$。

这就是为什么 RoPE 能够把绝对位置 $m$ 和 $n$ 的注意力计算，在数学上天衣无缝地转化为**完全依赖于相对距离 $(m - n)$ 的余弦函数**。


## 12. q*k_T怎么是余弦函数相乘，其中的m和n，θ分别表示什么意思
你的问题问到了 RoPE 旋转位置编码最核心的“连接点”！

我们一步步拆解：**符号究竟代表什么**，以及**为什么向量相乘（$q \cdot k^\top$）会变成三角（余弦）函数的乘积与相加**。

---

### 一、 先明确 $m$、$n$ 和 $\theta$ 到底是什么

在注意力计算中，我们有两个处于不同位置的 Token：

1. **$m$（Query 的绝对位置）**：
* 代表“当前提问的词”在句子里的索引序号。比如句子第 4 个词，则 $m = 4$。


2. **$n$（Key 的绝对位置）**：
* 代表“被检索的词”在句子里的索引序号。比如句子第 1 个词，则 $n = 1$。


3. **$\theta$（基准旋转角 / 旋转频率）**：
* 代表**位置每增加 1，向量旋转的固定基础角度**（单位：弧度）。
* 比如我们在上一步推导中设定的 $\theta = 30^\circ$（即 $\frac{\pi}{6}$）。



因此：

* **$m\theta$** 就是 Query 词因为处于第 $m$ 个位置，一共被旋转的总角度；
* **$n\theta$** 就是 Key 词因为处于第 $n$ 个位置，一共被旋转的总角度。

---

### 二、 为什么 $q \cdot k^\top$ 会算出余弦函数？

重点来了：**余弦函数并不是凭空出现的，它是我们将向量做“旋转”之后算出来的必然结果。**

我们看看 $q$ 和 $k$ 在旋转前后的变身过程：

#### 1. 旋转前的“原始向量”

假设未加位置编码时，语义向量 $q$ 和 $k$ 为二维向量（为了推导直观，上一步例子中设为了 $[1, 0]$）：

* $q = [q_1, q_2]$
* $k = [k_1, k_2]$

#### 2. RoPE 乘上旋转矩阵（注入位置）

RoPE 的规定是：**把向量乘以旋转矩阵 $\mathbf{R}$**。

* **对于 Query（位置为 $m$）：**
旋转后的向量 $\tilde{q}_m = \mathbf{R}_{m\theta} \cdot q$

$$\tilde{q}_m = \begin{pmatrix} \cos(m\theta) & -\sin(m\theta) \\ \sin(m\theta) & \cos(m\theta) \end{pmatrix} \begin{pmatrix} q_1 \\ q_2 \end{pmatrix} = \begin{pmatrix} q_1 \cos(m\theta) - q_2 \sin(m\theta) \\ q_1 \sin(m\theta) + q_2 \cos(m\theta) \end{pmatrix}$$


* **对于 Key（位置为 $n$）：**
旋转后的向量 $\tilde{k}_n = \mathbf{R}_{n\theta} \cdot k$

$$\tilde{k}_n = \begin{pmatrix} \cos(n\theta) & -\sin(n\theta) \\ \sin(n\theta) & \cos(n\theta) \end{pmatrix} \begin{pmatrix} k_1 \\ k_2 \end{pmatrix} = \begin{pmatrix} k_1 \cos(n\theta) - k_2 \sin(n\theta) \\ k_1 \sin(n\theta) + k_2 \cos(n\theta) \end{pmatrix}$$



可以看到，旋转后的向量**坐标里面自然就包含了 $\cos$ 和 $\sin$ 项**。

---

#### 3. 计算 $q \cdot k^\top$（内积）

现在计算 Softmax 之前的注意力得分（内积）：即用 $\tilde{q}_m$ 的第一个坐标乘 $\tilde{k}_n$ 的第一个坐标，加上第二个坐标乘第二个坐标。

为清晰展示，在上一步的简化推导中，我们设了 **$q = [1, 0]$** 且 **$k = [1, 0]$**（即 $q_1=1, q_2=0, k_1=1, k_2=0$）：

代入坐标：

* $\tilde{q}_m$ 简化为 $\begin{pmatrix} \cos(m\theta) \\ \sin(m\theta) \end{pmatrix}$
* $\tilde{k}_n$ 简化为 $\begin{pmatrix} \cos(n\theta) \\ \sin(n\theta) \end{pmatrix}$

把两个向量做点积（对应项相乘再相加）：


$$\text{Score} = \tilde{q}_m \cdot \tilde{k}_n^\top = \underbrace{\cos(m\theta) \cdot \cos(n\theta)}_{\text{X 坐标相乘}} + \underbrace{\sin(m\theta) \cdot \sin(n\theta)}_{\text{Y 坐标相乘}}$$

---

### 三、 完整版（即便 $q, k$ 不是 $[1, 0]$）

即使 $q_1, q_2, k_1, k_2$ 是任意实数，展开之后把带 $\cos$ 和 $\sin$ 的项整理提公因式，结果依然是：

$$\tilde{q}_m \cdot \tilde{k}_n^\top = (q_1 k_1 + q_2 k_2) \cdot \cos((m - n)\theta) + (q_2 k_1 - q_1 k_2) \cdot \sin((m - n)\theta)$$

看到了吗？

* **前半部分 $(q_1 k_1 + q_2 k_2)$**：完全是原始语义向量 $q$ 和 $k$ 的内积（纯语义相关性）；
* **后半部分 $\cos((m - n)\theta)$**：只与两者的相对距离 $m - n$ 有关！

### 核心结论

1. **$m$ 和 $n$**：代表两个 Token 在文本中的**排队序号**（绝对位置）。
2. **$\theta$**：代表位置每跨越一个 Token，向量在平面上**旋转的角度**。
3. **为什么是余弦相乘**：因为旋转动作（旋转矩阵）本身就是用 $\sin$ 和 $\cos$ 表达的。当你将两个旋转后的向量做点积（$q \cdot k^\top$）时，向量坐标里的三角函数自然相乘，并借由三角恒等式组合出了包含相对距离 $(m - n)$ 的余弦结果！

4. ## 13. q,k不是[1,0]时推导公式，另外θ怎么选择？
5. ### 一、 当 $q, k$ 不是 $[1, 0]$ 时的完整推导

我们引入任意具体的语义向量：

* **Query 向量**：设 $q = [2, 1]^\top$（位置为 $m$）
* **Key 向量**：设 $k = [1, 3]^\top$（位置为 $n$）
* **基础旋转角**：$\theta = 30^\circ = \frac{\pi}{6}$

在没有注入位置信息前，纯语义的点积为：


$$q \cdot k^\top = (2 \times 1) + (1 \times 3) = 5$$

---

#### 1. 写出旋转矩阵 $\mathbf{R}$ 并计算旋转后的向量

**对 Query（位置 $m$）：**


$$\tilde{q}_m = \mathbf{R}_{m\theta} \cdot q = \begin{pmatrix} \cos(m\theta) & -\sin(m\theta) \\ \sin(m\theta) & \cos(m\theta) \end{pmatrix} \begin{pmatrix} 2 \\ 1 \end{pmatrix} = \begin{pmatrix} 2\cos(m\theta) - 1\sin(m\theta) \\ 2\sin(m\theta) + 1\cos(m\theta) \end{pmatrix}$$

**对 Key（位置 $n$）：**


$$\tilde{k}_n = \mathbf{R}_{n\theta} \cdot k = \begin{pmatrix} \cos(n\theta) & -\sin(n\theta) \\ \sin(n\theta) & \cos(n\theta) \end{pmatrix} \begin{pmatrix} 1 \\ 3 \end{pmatrix} = \begin{pmatrix} 1\cos(n\theta) - 3\sin(n\theta) \\ 1\sin(n\theta) + 3\cos(n\theta) \end{pmatrix}$$

---

#### 2. 计算点积 $\tilde{q}_m \cdot \tilde{k}_n^\top$

将两个向量的对应坐标相乘再相加：

$$\begin{aligned} \tilde{q}_m \cdot \tilde{k}_n^\top &= \underbrace{[2\cos(m\theta) - \sin(m\theta)] \cdot [\cos(n\theta) - 3\sin(n\theta)]}_{\text{X 坐标相乘}} \\ &+ \underbrace{[2\sin(m\theta) + \cos(m\theta)] \cdot [\sin(n\theta) + 3\cos(n\theta)]}_{\text{Y 坐标相乘}} \end{aligned}$$

展开第一部分（X 坐标相乘）：


$$2\cos(m\theta)\cos(n\theta) - 6\cos(m\theta)\sin(n\theta) - \sin(m\theta)\cos(n\theta) + 3\sin(m\theta)\sin(n\theta)$$

展开第二部分（Y 坐标相乘）：


$$2\sin(m\theta)\sin(n\theta) + 6\sin(m\theta)\cos(n\theta) + \cos(m\theta)\sin(n\theta) + 3\cos(m\theta)\cos(n\theta)$$

将两部分合并并归类同类项：

* **合并 $\cos(m\theta)\cos(n\theta) + \sin(m\theta)\sin(n\theta)$ 项：**

$$(2 + 3) \cdot [\cos(m\theta)\cos(n\theta) + \sin(m\theta)\sin(n\theta)] = 5 \cdot \cos((m - n)\theta)$$


* **合并 $\sin(m\theta)\cos(n\theta) - \cos(m\theta)\sin(n\theta)$ 项：**

$$(-1 + 6) \cdot [\sin(m\theta)\cos(n\theta) - \cos(m\theta)\sin(n\theta)] = 5 \cdot \sin((m - n)\theta)$$



*(这里套用了正弦差角公式：$\sin(\alpha - \beta) = \sin\alpha\cos\beta - \cos\alpha\sin\beta$)*

最终化简得到：


$$\tilde{q}_m \cdot \tilde{k}_n^\top = 5 \cos((m - n)\theta) + 5 \sin((m - n)\theta)$$

#### 结论

1. 前面的系数 $5$ 来自于纯语义信息的计算组合（$q_1 k_1 + q_2 k_2 = 5$ 以及 $q_2 k_1 - q_1 k_2 = 5$）。
2. 三角函数部分**完全只由相对距离 $(m - n)$ 决定**，绝对位置 $m$ 和 $n$ 依然被完美消去。

---

### 二、 $\theta$（基准旋转角 / 频率）是如何选择的？

在大模型（如 LLaMA、Qwen）中，向量维度往往不是二维，而是比如 $d = 128$（单个 Head 的维度）。这时 RoPE 会将 128 维分成 **64 个独立的二维子空间**（两两一组），每一组分配一个不同的 $\theta_i$。

#### 1. 经典标准公式（Transformer / LLaMA 原版）

每个维度对 $i \in \{1, 2, \dots, d/2\}$ 的角度步长公式为：

$$\theta_i = \mathbf{Base}^{-2(i - 1)/d}$$

* **$\mathbf{Base}$（基数）**：原论文和 LLaMA 1/2 默认设为 **$10000$**。
* **维度编号 $i$**：从 $1$ 到 $d/2$。

#### 2. 为什么这样设计 $\theta$？（高频与低频的分工）

我们可以看一下不同维度上 $\theta_i$ 的数值变化特征：

* **前几维（$i$ 很小，低维度通道 / 高频通道）：**
* $\theta_i \approx 1$（旋转极其剧烈）。
* **作用**：位置每增加 1，向量角度就剧烈旋转。这种“敏锐”的旋转对**相邻 Token 之间的相对顺序和局部语法**极其敏感。


* **后几维（$i \approx d/2$，高维度通道 / 低频通道）：**
* $\theta_i \approx \frac{1}{10000}$（旋转极其缓慢）。
* **作用**：位置增加几千，向量也只旋转一小角。这保证了在**长距离（比如相隔几千字）文本**中，注意力得分不会因为频繁旋转而产生高频震荡失真，从而稳定捕获全局上下文。



#### 3. $\mathbf{Base}$ 的演进：长文本大模型中如何调大 $\mathbf{Base}$？

随着上下文窗口从 4k 扩展到 32k、128k 甚至更高，固定 $\mathbf{Base} = 10000$ 会使得最慢的旋转通道在极长位置上依然会“转圈圈过头”（发生相位重叠）。

因此，现代大模型普遍通过增大 $\mathbf{Base}$ 值来“拉长旋转周期”：

* **LLaMA 2 (4k 上下文)**：$\mathbf{Base} = 10,000$
* **CodeLLaMA (16k 上下文)**：$\mathbf{Base} = 1,000,000$
* **LLaMA 3 (8k -> 128k 上下文)**：$\mathbf{Base} = 500,000$

将 Base 放大可以**降低所有通道的旋转速率**，从而让模型在处理更长的文本位置索引时，旋转相位依然稳定在训练好的几何区间内。

## 总结
### 面试核心 Q&A 总结

#### Q1：什么是位置编码？Transformer 为什么必须用它？

* **回答**：
* **定义**：位置编码就是给输入的词贴上“序号标签”。
* **必要性**：Transformer 的自注意力机制（Self-Attention）是并行计算的，默认没有词序和方向概念（“张三打李四”和“李四打张三”算出来一样）。它抛弃了自带时序的传统循环结构（RNN），因此必须通过位置编码显式补充位置信息。



---

#### Q2：常见的位置编码有哪几种类型？

* **回答**：
1. **绝对位置编码**：直接给每个词绑定唯一的绝对序号向量。
* *正弦/余弦周期编码*（原版 Transformer）：通过三角函数计算固定向量。
* *可学习编码*（如 BERT、GPT-2）：把位置向量当作模型参数去训练。


2. **相对位置编码**：不关注绝对序号，只关注词与词之间的距离（$m - n$）。
* *RoPE 旋转位置编码*（如 LLaMA、Qwen）：将向量旋转特定角度，求点积时自动提取相对距离。
* *ALiBi*（如 Bloom）：在注意力矩阵上直接施加与距离成正比的负偏置惩罚。





---

#### Q3：在长文本大模型中，为什么 RoPE 和 ALiBi 比传统绝对位置编码的外推能力更好？

* **回答**：
* **传统绝对位置编码差**：超长位置在训练时没见过（可学习编码）或会产生异常的向量内积分布（正弦编码），导致注意力机制崩溃。
* **RoPE 优势**：数学逻辑严密（只与相对距离有关），配合插值算法（如 YaRN、NTK-aware）压缩旋转频率，可平滑扩展到数十万 Token。
* **ALiBi 优势**：直接根据距离拉大惩罚，不依赖位置向量，支持零样本（Zero-shot）直接外推更长文本。



---

#### Q4：ALiBi 外推好，为什么主流大模型（LLaMA 3、Qwen 2.5）依然普遍选择 RoPE？

* **回答**：
1. **无强行限制，表达上限更高**：ALiBi 强行压制远距离 Token，易误杀“大海捞针”或长代码中的关键信息；RoPE 不强行惩罚，模型可自由学习长距离依赖。
2. **长文本技术补齐短板**：RoPE 配合 YaRN/插值技术轻松扩展至 128k+，补齐了外推劣势。
3. **生态与多模态兼容**：RoPE 易扩展到 2D/3D（处理图像/视频），且原生适配 FlashAttention 算子加速。



---

#### Q5：如何通俗理解 RoPE 的原理？

* **回答**：
* **核心直觉（钟表指针）**：将词向量看作平面上的“指针”。指针的初始方向代表**语义**，指针旋转的角度代表**位置**。位置每增加 1，指针就顺时针旋转一个固定角度 $\theta$。
* **算注意力（点积）**：两个指针求点积时，几何上算的是夹角。绝对位置的角度在相减时被销毁，只留下相对距离 $(m - n) \theta$ 的余弦值。



---

#### Q6：RoPE 是如何通过旋转公式把绝对位置转化为相对位置的？

* **回答**：
1. **旋转注入**：位置 $m$ 的 Query 向量旋转 $m\theta$ 后，坐标自带 $\cos(m\theta)$ 与 $\sin(m\theta)$。
2. **点积展开**：计算 $q_m \cdot k_n^\top$ 时，两向量坐标相乘相加。
3. **三角合并**：利用两角差公式 $\cos\alpha\cos\beta + \sin\alpha\sin\beta = \cos(\alpha - \beta)$，直接化简出仅包含相对距离的 $\cos((m - n)\theta)$ 项。



---

#### Q7：RoPE 中的旋转角 $\theta$ 是怎么选择和设计的？

* **回答**：
* **高低频分工**：高维向量会两两拆分为多个二维子空间，赋予不同的旋转步长 $\theta_i = \mathbf{Base}^{-2(i-1)/d}$（$\mathbf{Base}$ 默认 10000）。
* **前几维（高频通道）**：转得快，负责捕捉局部相邻词的语法关系。
* **后几维（低频通道）**：转得极慢，负责稳定长距离的全局上下文。
* **长文本扩展**：将 $\mathbf{Base}$ 调大（如 10,000 $\rightarrow$ 500,000）可以拉长旋转周期，防止超长上下文在旋转时发生相位重叠。












* **典型代表（如 RoPE 旋转位置编码 / ALiBi / T5 位置偏置）：**
* **RoPE（Rotary Position Embedding，当前主流大模型如 LLaMA / Qwen 标配）**：通过复数旋转矩阵将位置信息隐式注入到 Query 和 Key 向量的内积计算中，既保留了相对位置的几何性质，又极大地提升了模型对外推（处理更长上下文）的能力。
* **ALiBi（Attention with Linear Biases）**：在注意力得分矩阵上直接加上一个与距离成正比的负偏置惩罚，距离越远注意力关注度越低，无需额外训练位置向量，外推能力强。
